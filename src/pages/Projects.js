import { Box } from "@chakra-ui/react";
import Header from "../components/Header";

const Projects = () => {
  return (
    <Box p={3} m={3} pt={0}>
      <Header text="Projects" component="span" />
    </Box>
  );
};

export default Projects;
