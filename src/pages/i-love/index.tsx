import Header from '../../components/header';

const ILove = (props: any) => {
   return (
      <section className={`mt-4`}>
         <Header className={`text-light text-bold`} text={`ILove`} />
         <p className={`text-light`}>ILove</p>
      </section>
   );
};

export default ILove;
